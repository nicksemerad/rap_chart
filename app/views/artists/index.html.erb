<h1>All Artists</h1>
<%= link_to 'New Artist', new_chart_artist_path(@chart)%>
<ul>
  <% @artists.each do |artist|%>
    <li>
    <%= link_to artist.name, artist_path(artist) %>
    </li>
  <% end %>

</ul>